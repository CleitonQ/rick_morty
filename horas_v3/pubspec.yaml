name: horas_v3
description: "Um projeto para gerenciar as nossas horas"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.2.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2

  # Firebase
  firebase_core: ^3.15.2
  firebase_auth: ^5.7.0
  cloud_firestore: ^5.0.1
  firebase_messaging: ^15.2.10

  google_sign_in: ^6.2.1

  # Utilitários
  uuid: ^4.3.3
  mask_text_input_formatter: ^2.8.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

dependency_overrides:


flutter:
  uses-material-design: true
